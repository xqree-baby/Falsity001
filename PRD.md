# PRD.md — 产品需求文档

> Day 4 产出。本文档回答「产品要做成什么样、做到什么程度算合格」。技术选型见 Day 5，不在此文讨论。
>
> 上游输入：research.md（Day 3）。

**项目（暂定名）**：社会认知 / 职场经验分享网站——MVP 版
> 项目名称未定，本档先用描述性暂定名；定名后再统一替换。

**一句话**：由完成公司认证的职场前辈分享进入社会的经验，普通人通过提问和评论参与交流的网站。

---

## 一、背景与目标

### 背景
初入社会的人缺少可靠的社会认知来源：知乎门槛放宽后内容鱼龙混杂，脉脉匿名区风险高，在行 1 对 1 模式太重（详见 research.md 第二节）。市场上缺一个「发言权有门槛、内容轻量、经验为主」的产品。

### 目标
交付一个能走通完整核心链路的 MVP：

> 认证用户发布观点帖 → 普通人在首页浏览 → 进入详情页提问/评论 → 未认证用户发布的提问帖经过审核后展示。

### 成功标准（本项目层面）
- 一个从未见过本产品的人打开网站，5 分钟内能明白：这是谁在分享、分享什么、自己能做什么。
- 「认证用户发观点、未认证用户提问需审核」这条核心规则在界面上清晰可见，无需解释。

## 二、目标用户

| 用户 | 特征 | 能做什么 |
|---|---|---|
| 认证前辈 | 完成公司认证的职场人（MVP 用写死数据模拟） | 发观点帖、发提问帖、评论 |
| 未认证普通人 | 无认证的访客/注册用户 | 评论、发提问帖（需审核后展示） |

## 三、功能需求（MVP 口径）

以下均为 MVP 范围，描述「用户看到什么、操作后发生什么」，不含技术实现。

### F1 首页（帖子列表）
- 展示帖子列表，每条帖子显示：标题、类型（观点 / 提问）、发布者身份（认证用户显示认证标识，如「某公司 · 已认证」）。
- 未通过审核的提问帖不出现在列表中。
- 未认证用户发布的提问帖在列表中标明「已审核」状态；认证用户的帖子不经审核，无此标记。

### F2 帖子详情页
- 完整展示帖子内容、发布者身份、类型。
- 下方展示评论列表，评论显示评论者与内容。

### F3 发帖
- 认证用户：可选择「观点」或「提问」类型发布，发布后直接展示，**无需审核**（信任来自公司认证本身）。
- 未认证用户：只能选择「提问」类型；发布后进入待审核状态，提示用户「发布成功，等待审核」。
- 未认证用户界面不提供「观点」选项。

### F4 评论
- 所有用户（认证与未认证）均可在帖子下发评论。
- 评论无需审核，发布后立即显示。

### F5 审核模拟
- **审核对象仅为未认证用户发布的提问帖**；认证用户的帖子不经审核。
- 提供一个审核入口（管理员视角页面即可），可查看待审核提问帖，执行「通过 / 拒绝」。
- 通过后帖子出现在首页；拒绝则不展示（MVP 可不提供拒绝原因）。
- 审核规则为简单关键字拦截 + 人工点击，不涉及 AI。

## 四、范围界定（本期不做）

砍掉以下 6 项及理由——这是本 PRD 最重要的决策之一：

| # | 不做的事 | 为什么砍 |
|---|---|---|
| 1 | 用户注册登录体系 | MVP 用写死的认证/未认证身份模拟。注册登录本身不验证「核心规则是否成立」，却要引入密码存储、会话管理等一整套复杂度，属高成本低验证价值 |
| 2 | 真实公司认证流程 | 同上，认证只影响「谁能发观点」，写死数据足以验证这条规则；真实认证涉及材料审核流程，远超 21 天计划能力 |
| 3 | AI 自动审核 | 审核要验证的是「有这道闸门」这个机制，不是审核的智能程度；MVP 用关键字规则 + 人工点击即可验证，AI 是后续增强项 |
| 4 | 手机端适配 | 网页按桌面浏览器设计。适配意味着额外一整套样式工作，且不影响核心链路的验证 |
| 5 | 数据库 | 数据先存在内存/本地文件中，Day 23 才引入 .env 与数据库配置；提前引入只增加调试面 |
| 6 | 点赞、私信、关注等一切社交功能 | 与「分享经验 + 提问交流」的核心链路无关——点赞也不做，交互轻量化反而突出内容本身；一律不做，防止范围蔓延 |

## 五、验收标准

每条均可亲眼验证：

### 文档与流程
- [ ] AC1：PRD.md 已提交入库（仓库中可见）。
- [ ] AC2：本文档含验收标准章节（即本节）。
- [ ] AC3：范围界定中每一项「不做」都附有理由，无未解释的砍项。

### 功能（以 MVP 实现后的界面为验收对象）
- [ ] AC4：打开首页可见帖子列表，每条帖子可见类型（观点/提问）与发布者身份；认证用户带认证标识。
- [ ] AC5：点击帖子可进入详情页，看到完整内容与评论（评论区可为空）。
- [ ] AC6：认证用户发帖时可选「观点」或「提问」，发布后帖子出现在首页。
- [ ] AC7：未认证用户发帖界面没有「观点」选项，只能发提问帖；发布后看到「等待审核」提示，且该帖不出现在首页。
- [ ] AC8：待审核帖子在审核入口通过后出现在首页；拒绝后不出现。
- [ ] AC9：任意用户评论后，评论立即显示在帖子详情页。

### 界限（防止偷偷超范围）
- [ ] AC10：MVP 交付物中不出现注册登录页面、真实认证流程、AI 审核、手机端适配、数据库配置、点赞/私信/关注功能中的任何一项。

---

## 附：后续迭代方向（不属于本期）

- 真实注册登录与公司认证
- AI 辅助审核
- 数据库持久化（Day 23 起）
- 移动端适配
- 点赞等互动功能（视核心链路验证结果决定）

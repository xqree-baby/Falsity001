# 队友自查清单（TEAM_CHECKLIST.md）

这份清单是 `AGENTS.md` 里两条规则的**可直接执行版**：队友不用读完整份规则文件，照着打勾就能用。
每次交付或推送前各过一遍，**一条打不了勾就先别交**。

> 来源：`AGENTS.md` 五.5（提交前先本地验证）、`AGENTS.md` 八（不许编，拿不准就标出来）、`AGENTS.md` 五.6（推送报网络错误先查远端）。
> 这几条规则日后有改动，记得回来同步这份清单。

## A 卡：推之前，先自己看一遍真东西

**用在**：`git push` 之前。**耗时**：约 30 秒。

- [ ] 这次要推的东西里，有「得打开网页才看得见」的部分吗？（图片、图表、排版、页面）
      —— 没有，直接跳过 A2–A5。
- [ ] 我**真的在本地打开/渲染过它**了吗？—— 标准是「我看见了」，不是「我觉得应该没问题」。
- [ ] 我看的是**产物本身**吗？（真渲染出的图、真打开的页面）—— 不是源码、不是中间文件、不是别人给的截图。
- [ ] 之前**失败过的地方，改完又重跑了一次**吗？—— 「改完直接推」不算过。
- [ ] 本地验证**够不到**的部分（比如只有 GitHub 上才有的渲染差异），写进提交说明了吗？
- [ ] 没过就别推。改到过了再提交。
- [ ] 推送**报了网络错误**（连接被重置/超时）？—— **不许直接重推**：先用 `git ls-remote`（或 GitHub API）看远端到底有没有这个提交。**报错不等于没推上**——数据可能已送达、只是回执没回来。确认远端真没有，才重推。

**不合格示范**：「我检查过了，图应该没问题。」—— 没有任何能看见的东西。
**合格示范**：「本地渲染输出 `status: ok`，截图在 `tmp/render.png`。」—— 有路径、有结果，别人能自己复现。
**不合格示范（报错时）**：「推送报错了，我再推一次试试。」—— 没查远端就重推。
**合格示范（报错时）**：「报了连接重置，先 `ls-remote` 查了远端确实没有这个提交，才重推。」

## B 卡：拿不准就标出来，别编

**用在**：任何文档、报告、结论交出去之前。

- [ ] 这段话里的**每个数字、每个出处、每个结论**，我都能指出它从哪来吗？—— 不能，就改成「待确认」，并补一句「怎么核实」。
- [ ] 我有没有把**推测写成事实**？—— 「大约是」「我理解是」要显式写出来，别混在肯定句里。
- [ ] 我说「**已完成**」时，能立刻给出一件**让对方自己能验证**的东西吗？（文件路径 / 页面地址 / 命令输出）—— 给不出，就改成「做到哪一步 + 还差什么」。
- [ ] 标了「待确认」的地方，有没有**同时给出核实办法**？—— 只标不说明，等于把活儿扔回给对方。
- [ ] 我有没有为了让结果好看，**偷偷删掉**不确定的部分？—— 删可以，结尾要说明删了什么。

**不合格示范**：「已全部完成。」（无证据）／「据统计 87% 的用户……」（无来源）
**合格示范**：「页面部分已完成，截图见 `xxx.png`；数据只找到 2024 年口径，2025 年暂无来源 → 待确认，可查某某官网年报。」

## 群里一句话版

> 推之前先自己看一遍真东西，**看见才算过**；
> 拿不准就写「待确认」+ 怎么查，**别编**，别把「我觉得」说成「已完成」。

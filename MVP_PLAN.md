# MVP 计划（Day 7）

> 项目名暂未定型，本文档以「项目」代称。Day 8+ 再正式取名。
> 本文件是 Day 7 当天的事实清单，不是长期架构文档。

## 1. 目标

让项目能在本地跑起来，浏览器打开 localhost 能看到 MVP 的基本页面交互，把"PRD 的产品逻辑"用最简方式落到能跑通的版本里。

不做完整功能，只做能演示核心规则的最小集合。

## 2. 技术栈（与 TECH_DESIGN 一致）

- 后端：Node.js + Express
- 数据：内存 + `posts.json`（不引入数据库）
- 前端：原生 HTML + CSS + JS（不引入框架）
- 启动：Node 进程常驻，浏览器访问 `http://localhost:8080/`

## 3. 身份模型（简化版）

PRD 要求按"是否认证"决定发哪种帖。Day 7 不做登录，所以身份用 URL 参数切换：

- `?as=anonymous`（默认）：未认证用户，只能发提问帖，发布后进「待审核」
- `?as=verified`：认证用户，两类都能发，发布后直接上首页

后端读取 `req.query.as` 判断身份。所有写操作（发评论、发帖、审核）都走 POST，由后端按身份判定，**前端不参与判定**（TECH_DESIGN 一.1.2：规则必须落在后端）。

种子数据里至少包含一个认证用户「已认证」标识位 + 一个未认证用户，方便直接演示两种身份的差异。

## 4. 页面清单

| 路径 | 用途 | 关键交互 |
|---|---|---|
| `/` | 首页 | 列帖子（标题 + 类型 + 发布者 + 认证标识） |
| `/post/:id` | 详情页 | 完整内容 + 评论列表 + 提交评论表单 |
| `/submit` | 发帖页 | 身份切换（默认 anonymous）+ 类型选择（受身份约束）+ 提交 |
| `/admin` | 审核入口 | 待审核列表 + 通过 / 拒绝 按钮 |

## 5. 今天做的具体项

| # | 文件 / 内容 | 说明 |
|---|---|---|
| 1 | `server.js` | Express；4 个页面的路由 + 4 个写接口 + 读写文件 |
| 3 | `posts.json` | 种子数据（2–3 条认证帖 + 1 条待审核帖 + 1–2 条评论） |
| 4 | `public/index.html` | 首页列表 |
| 5 | `public/post.html` | 详情页（带评论） |
| 6 | `public/submit.html` | 发帖页 |
| 7 | `public/admin.html` | 审核入口 |
| 8 | `public/style.css` | 4 页共用基础样式（不引入第三方） |
| 9 | `README.md` | 启动命令、端口、URL、试不同身份的方法 |
| 10 | `.gitignore` 核对 | `.env` 与 `.workbuddy/` 已在排除 |
| 11 | 本地跑起来 + 你浏览器看 | 验收步骤 |

## 6. 今天不做的项（守住边界）

清单 + PRD 砍功能 + Day 23 才引入的功能：

1. 注册 / 登录 / 会话 / 密码
2. 真实公司认证流程（仅靠 URL 切身份模拟）
3. AI 自动审核
4. 手机端样式（只做桌面浏览器可见）
5. 数据库（Day 23 才引入）
6. 点赞 / 私信 / 关注
7. 支付、复杂缓存、第三方服务
8. 项目正式命名（Day 8+ 再定）

## 7. PRD 验收标准覆盖计划

| AC | 内容 | Day 7 状态 |
|---|---|---|
| AC1–3 | PRD 入库 + 含验收章节 + 砍功能附理由 | ✅ 已满足（Day 4 完成） |
| AC4 | 首页可见类型 + 认证标识 | ✅ 今天做 |
| AC5 | 详情页可看完整内容 + 评论 | ✅ 今天做 |
| AC6 | 认证用户可选观点/提问，发布即显示 | ✅ 今天做 |
| AC7 | 未认证用户无「观点」选项，发布后看到等待审核、首页不展示 | ✅ 今天做 |
| AC8 | 审核入口通过 → 上首页；拒绝 → 不展示 | ✅ 今天做 |
| AC9 | 评论立即显示 | ✅ 今天做 |
| AC10 | MVP 不出现砍功能列表中的任何一项 | ✅ 写文件时人工核对 |

## 8. 验收方式（Day 7 完成标准）

按今日任务清单：

- 页面能在本地启动打开：`python -m http.server` 或 `node server.js` 二选一能起服务，浏览器看到内容
- 运行命令已存档：`README.md` 写清
- 改动已提交：commit + push
- 截图：浏览器地址栏以 `localhost` 开头，页面能认出是本项目

## 9. 后续待办（不在今天范围）

- Day 8+：项目正式命名、拆前端资源、引入包管理（package.json + npm install）
- Day 23：替换 `posts.json` 为真实数据库连接
- 待办：发帖页加输入校验、错误提示更友好；审核入口加搜索/筛选
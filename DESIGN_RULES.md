# 前端设计检查规则（Day 9 建立）

依据：Day 8 定的全站视觉规范（见 public/style.css 顶部注释）。
用途：每次改完前端，对照本清单逐条检查，能亲眼确认的不合格才算问题。

## 一、文字对比度

| 规则 | 怎么查 | 不合格的样子 |
|---|---|---|
| 正文（≤14px）与背景对比度 ≥ 4.5:1 | 取文字色和背景色的十六进制值，用对比度公式计算 | 灰字放在灰底上，截图放大后发虚 |
| 大字标题（≥18px 加粗 / ≥24px）对比度 ≥ 3:1 | 同上 | 淡色标题浮在浅底上 |
| 禁止色值清单外的颜色出现在文字/背景上 | 全局搜 `#` 十六进制色值，逐个对照色板 | 私自加色（色板 7 色 + 错误粉 3 档，见 style.css `:root`） |
| 错误场景只用淡粉系（#fceef2 / #b04a66 / #f0c6d2） | 搜错误提示相关 class | 用红色/黄色报错 |

## 二、间距

| 规则 | 怎么查 | 不合格的样子 |
|---|---|---|
| 所有间距取 4 的倍数（4/8/12/16/24/32） | 查 CSS 里 padding/margin/gap 的数值 | 出现 13px、17px 这类离群值 |
| 卡片内边距全站统一 | 截图对比首页卡片 / 表单 / 侧栏内边距 | 同类卡片一个 16px 一个 18px |
| 标题与正文间距 < 正文与下一区块间距（分组感） | 看截图：标题应「贴着」自己的内容 | 标题悬在两个区块正中间，不知道属于谁 |

## 三、对齐

| 规则 | 怎么查 | 不合格的样子 |
|---|---|---|
| 同一行元素底边/中线对齐 | 截图放大看行内元素 | 徽章和文字上下错开 |
| 页面左右留白对称 | 看截图两侧空隙 | 左边 20px 右边 35px |
| 顶栏内容与正文内容左边缘对齐 | 顶栏行 max-width 与正文 container 是否同宽、同居中 | 顶栏文字比正文缩进更深或更浅 |

## 四、移动端（窄屏）

| 规则 | 怎么查 | 不合格的样子 |
|---|---|---|
| 960px 以下不允许横向滚动 | 浏览器窗口缩到 ~375px 宽截图 | 页面能左右晃、内容被裁掉 |
| 触控目标（按钮/链接）高度 ≥ 32px | 截图量 | 按钮小到点不中 |
| 单行文本超长要截断或换行，不许撑破容器 | 造一个超长标题看渲染 | 长标题把卡片顶破 |

## 五、色板（Day 8 拍板，改动需走确认流程）

- 主色系 7 种：白 / 浅蓝灰底 #f2f5f9 / 边框 #d9e0ea / 主蓝 #2f6fb2 / 深蓝 #1d4f8c / 青 #146e82 / 冷灰（正文 #2b3442、次要 #64748b）
- 蓝、青的浅色底是同色相淡色，不另算新颜色
- 错误提示专用淡粉三档，是唯一例外色
- 改色只动 style.css 顶部 `:root` 变量，不许在别处写死色值
